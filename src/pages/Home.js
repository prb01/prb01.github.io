import profilePic from "../assets/img/profile.png"

const Home = () => {
  return (
    <section className="fixed bottom-10 xl:static xl:h-full xl:flex">
      <img src={profilePic} />
    </section>
  )
}

export default Home
